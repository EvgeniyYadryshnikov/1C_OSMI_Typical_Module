Функция ПолучитьЗначениеПоля(Карта, Проверка = Ложь) Экспорт
	
	Результат = "";
	
	Попытка
		Выполнить(ТекстПрограммы);
	Исключение
		
		Если Проверка Тогда
			Возврат "Ошибка времени исполнения: " + ОписаниеОшибки();
		Иначе
			ЗаписьЖурналаРегистрации("ОСМИ.Ошибка", УровеньЖурналаРегистрации.Ошибка,,, ОписаниеОшибки());
			Возврат ЗначениеПоУмолчанию;
		КонецЕсли;
		
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции